name: Bug report
description: A crash, a wrong error, or output that is wrong on its own terms
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        If vep-rs runs cleanly but its output differs from Perl VEP's, use the **Output differs from Perl VEP** form instead. Report security vulnerabilities privately per [SECURITY.md](https://github.com/natera-open-source/vep-rs/blob/main/SECURITY.md), not here.
  - type: input
    id: version
    attributes:
      label: vep-rs version
      description: Output of `vep --version`, or the commit you built from.
      placeholder: vep 0.1.0
    validations:
      required: true
  - type: dropdown
    id: install
    attributes:
      label: How you installed it
      options:
        - Release binary
        - Built from source
    validations:
      required: true
  - type: input
    id: platform
    attributes:
      label: Platform
      description: OS and CPU. Release builds need AVX2 on x86_64; an illegal-instruction crash on an older CPU is covered under "CPU floor" in the README.
      placeholder: Ubuntu 24.04, x86_64
    validations:
      required: true
  - type: textarea
    id: command
    attributes:
      label: Command
      description: The full command line you ran.
      render: shell
    validations:
      required: true
  - type: textarea
    id: cache
    attributes:
      label: Cache and reference
      description: Assembly and Ensembl release, whether the JSON cache came from `vep-cache-builder` or from converting a Perl VEP cache, and the `--fasta` file if you passed one.
      placeholder: GRCh38, release 115, built with vep-cache-builder
    validations:
      required: true
  - type: textarea
    id: what-happened
    attributes:
      label: What happened
      description: What you expected, and what you got instead. Paste errors and log output as text, and add `--verbose` if it shows more.
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Input that reproduces it
      description: The smallest input you can share, ideally a few VCF lines. Do not post patient or other identifiable data. If the bug reproduces against the bundled corpus in `tests/golden/`, say so.
      render: text
