# Published measurement data

Every number in the vep-rs concordance and throughput results re-derives from the fifteen CSV files
in this directory and the side files under `transcript_set_parity/`. Nothing in the published results
depends on data that is not here. The provenance records under
[`docs/concordance-provenance/`](../../docs/concordance-provenance/) pin each run these files draw on:
the vep-rs sweep (a concordance record and a wall-time record sharing one `sweep_id`, the latter also
carrying the fastVEP and Perl VEP wall-time cells it reuses), the GRCh37 structural-variant reference
regeneration, the breakend batch replay, the cache-parity audit and the GRCh37 cache-113-versus-115
comparison. Each record carries the binary digests, dataset-inventory digest and per-machine verdicts
of its run.

| File                                     | What it holds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dataset_inventory.csv`                  | the eight datasets of Table S1 (six SNP/indel, two per-assembly structural-variant sets): assembly, the `suite` key every other file uses for the dataset, source release, and the input record count of each canonical VCF, which is what the wall times are measured against                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `f1_observations.csv`                    | vep-rs per-suite tuple counts: Perl, vep-rs and intersection, raw and post-mask. Every published F1 recomputes as 2I/(A+B)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `f1_observations_fastvep.csv`            | the same for fastVEP v0.3.0, plus its excluded swap-pair count per suite (`excluded_categories`) and the deduplicated cross-file `*_union` counts used for volume comparison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `f1_by_consequence_class.csv`            | per Sequence Ontology term, pooled over the six SNP/indel datasets, both engines: `vep_tuples`, `vep_only`, `engine_only`; the intersection is `vep_tuples - vep_only` and F1 is 2I/(A+B) with A = `vep_tuples`, B = I + `engine_only`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `f1_by_consequence_class_by_dataset.csv` | the same columns per (dataset, term), which the pooled file sums; each row's F1 re-derives the same way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `wall_times.csv`                         | 1,176 rows: the 20 per-machine observations for each of the 56 timed cells, plus one aggregate row per cell (`run_index=median_of_20`). Columns: `date`, `engine`, `engine_version`, `suite`, `assembly`, `instance_type`, `run_index`, `wall_time_sec`, `peak_rss_mb`, `pct_cpu`, `major_page_faults`, `minor_page_faults`, `cache_state`, `notes`. `pct_cpu` and the two page-fault counts are populated on the vep-rs and fastVEP per-machine rows only, `peak_rss_mb` on those two engines' per-machine and aggregate rows; no Perl VEP row carries a memory or CPU figure (`perl_bench.csv` is the Perl memory record)                                                                                                                                                                                                                                                                                                                                                                          |
| `perl_bench.csv`                         | the Perl VEP container-cgroup peak-memory probes, one run per input: four sites-only cells (`sites_only_canonical`) and the genotype-carrying 1000G high-coverage input on the timed host class (`with_genotypes`, with the cgroup limit, the `oom_kill` count and how the run ended in `notes`); single probe runs, not comparable to the wall-time medians. `pct_cpu`, `major_pf` and `minor_pf` are part of the schema and empty: a cgroup probe records memory, not CPU or paging                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `container_start.csv`                    | the 800 container start-up timings behind S4.5: 20 probes on each of the 40 Perl VEP machines of the structural-variant campaign, each a bare `docker run --rm <image> true` after the machine's timed cells, plus one aggregate row per architecture (`clone_run_index=median_of_clones`, the median over the 20 per-clone medians). Columns: `date`, `sweep_id`, `arch`, `instance_type`, `clone_run_index`, `image`, `kind`, `probe_index`, `wall_seconds`, `exit_code`, `notes` |
| `sv_concordance_by_category.csv`         | per-input-file structural-variant F1, raw and adjusted, for vep-rs and fastVEP, with the VEP, engine and intersecting tuple counts each value divides; 16 files per assembly (13 synthetic, 3 real-world); built by `../figures/make_sv_category_csv.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `discordance_taxonomy.csv`               | the five divergence classes carrying every adjusted-F1 exclusion: per (class, suite) the tuple count, how many pairs the mask removes and the resulting `mask_status`; built by `../figures/make_taxonomy_csv.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `start_retained_shapes.csv`              | per-dataset and pooled shapes of the VEP-only `start_retained_variant` tuples (the masked co-emission swap, four unmasked shapes and an `other` remainder), pinned to the sweep and binary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `transcript_set_parity.csv`              | per assembly, the transcript ids of the Ensembl GFF3 fastVEP builds from against the VEP-derived JSON cache: counts in both, in each only, and how many one-sided ids lie on the canonical contigs; where a contig of VEP's own Storable cache was listed, that the conversion dropped none; the classification of the canonical-contig GFF3-only ids against the Ensembl core database (`readthrough_tra` attribute, biotype `artifact`), which accounts for every one of them; the cache's transcripts split by whether fastVEP's loader admits their GFF3 feature type (`cache_transcripts_fastvep_admitted`, `cache_transcripts_fastvep_unloaded`, `fastvep_loader_version`); and, on the GRCh37 row, the Storable cache 113-versus-115 comparison (`storable_113_transcripts`, `storable_115_transcripts`, the identical and differing counts, `storable_113_only`, `storable_115_only`), whose `storable_113_115_sweep_id` names that cache-comparison run, not an engine measurement campaign |
| `fastvep_extra_tuples_by_transcript.csv` | per SNP/indel suite, fastVEP's one-sided tuples split by whether they name a transcript VEP's cache excludes (readthrough or artifact), a scaffold transcript, no feature, or another transcript, and VEP's one-sided tuples split by whether they name a transcript fastVEP's loader does not admit; the two one-sided totals equal `fastvep_tuples - intersection` and `perl_tuples - intersection` of `f1_observations_fastvep.csv`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `fastvep_zero_f1_shapes.csv`             | per SNP/indel suite and pooled, fastVEP's tuples on the five terms where its F1 is zero although it emits tuples, each filed as VEP's consequence set at a shifted key (location or allele differing), a different set at the same key, a different set on the same span, or no VEP tuple on that transcript; the four shapes sum to `fastvep_tuples_with_term`, and the pooled per-term totals equal the fastVEP `engine_tuples` of `f1_by_consequence_class.csv`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `code_metrics.csv`                       | per-crate lines of code and `#[test]` counts with a TOTAL row; built by `../figures/make_code_metrics_csv.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### `transcript_set_parity/`

The id-level basis for the counts in `transcript_set_parity.csv`:

| File                                                 | What it holds                                                                                                                                                                |
| ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `<assembly>_gff3_only_classes.tsv`                   | one row per GFF3-only transcript id with its contig, biotype, `readthrough_tra` flag and class (`readthrough`, `artifact`, `non_canonical_contig`)                           |
| `<assembly>_fastvep_unloaded.tsv`                    | one row per cache transcript whose GFF3 feature type fastVEP's loader does not admit, with its contig, feature type and biotype                                              |
| `<assembly>_cache_feature_types.csv`                 | the cache's transcripts per GFF3 feature type and whether fastVEP's loader admits that type                                                                                  |
| `storable_113_vs_115/summary_113_vs_115.json`        | the GRCh37 Storable cache 113-versus-115 transcript counts and the fields the content digest normalises                                                                      |
| `storable_113_vs_115/differing_113_115.tsv`          | the differing transcripts with contig and coordinates                                                                                                                        |
| `storable_113_vs_115/differing_113_115_classes.tsv`  | per differing transcript, which of `peptide`, `predictions` and `protein_features` differ, and whether the JSON cache's peptide matches the 113 build, the 115 build or both |
| `storable_113_vs_115/differing_113_115_classes.json` | the per-class and per-combination totals of that table and the JSON cache (`json_cache`) they were compared against                                                          |

## Recomputing the published statistics

**F1.** Pool `perl_tuples`, `rust_tuples` and `intersection` for the suites you want and take
`2I/(A+B)`. For an adjusted value on the SNP and indel suites, subtract that suite's masked pair count
(the sum of its `masked_pairs` rows in `discordance_taxonomy.csv`) from both raw denominators; the two
structural-variant rows carry their post-mask denominators in their own `adj_*` columns. Do not mix the
file-summed columns with the `*_union` columns: they are two different bases and a suite's raw and
adjusted F1 must never sit on two of them.

**Per-class F1.** In `f1_by_consequence_class.csv` and its per-dataset companion, a term's
intersection is `vep_tuples - vep_only`, the engine's total is that intersection plus `engine_only`,
and F1 is `2I/(A+B)` over those two totals; `intersection`, `engine_tuples`, `precision`, `recall` and
`f1` are stored as well so the arithmetic can be checked column by column. Term sets overlap, so one
tuple contributes to several terms and the per-class column does not pool back into the aggregate.

**Wall time.** Each cell's published figure is the median of its 20 per-machine observations. Rows with
`run_index=median_of_20` are the aggregates, and their `notes` field carries `p5`, `p25`, `p75`, `p95`,
`min`, `max` and `p95_p5_ratio`. All 56 cells use one percentile estimator, the Weibull plotting
position (R type 6), which is `statistics.quantiles(sorted(values), n=20, method="exclusive")` in
Python: index 0 is P5 and index 18 is P95. Every stored aggregate recomputes from that cell's own 20
observations, so the estimator never has to be inferred.

**The `notes` column.** A `;`-separated list of `key=value` pairs and bare tokens, each a fact about the
measurement itself. Every `wall_times.csv` row carries `methodology=cross_instance_parallel_clone`,
`sweep_id`, `arch`, `condition` (`sites_only` or `with_gt`), `local_nvme=true`, `n_timed` and `suite_id`;
a per-machine row adds `clone_run_index`, and an aggregate row adds the dispersion keys above plus
`n_clones` and `warmup_run_discarded_per_clone=1`. The `sweep_id` is the join key to the provenance
records, and a row belongs to exactly one sweep. In `f1_observations.csv` the `verdict` column carries
`n_tier`, `methodology`, `sweep_id` and `binary_x86` (the x86 build's digest prefix; the `binary_md5` column
is the ARM build's), and on the two structural-variant rows the file-summed basis (`sv_aggregation`), the
ground-truth set (`sv_ground_truth`, with `sv_gt_cache` on the GRCh37 row) and the `<CNV:TR>` swap-pair
total (`cnvtr_swap_pairs_total`); which tree the binaries were built from is recorded by the provenance
record, not by a row. In `f1_observations_fastvep.csv` the column carries
`suite`, `sweep_id`, the fastVEP version and `arch_independent`, and on the two structural-variant rows
the ground-truth set (`sv_per_vcf_ground_truth`), the file-summed basis (`sv_aggregation`), the
comparator, the symbolic-allele normalisation and `absent_from_cache`. In `perl_bench.csv` it carries the
probe kind (`perl_peak_memory_probe`, `single_probe_run_not_a_walltime_measurement`), the suite id, the
VEP invocation (`fork16_buffer5000`, `sites_only_canonical` or `with_genotypes` with `samples`), the
memory unit and source, the sample interval, the input and output record counts, the kernel, the
anonymous-memory maximum (`anon_max_mib`) on the sites-only rows, and on the genotype-carrying row the
cgroup limit, `oom_kill`, `end_reason`, `completed`, the wall-time cap and the input digest prefix. No
token records which run of the tooling wrote a row: the sweep id and the provenance record it names are
that trace.

**Figures.** `python3 manuscript/figures/generate_figures.py --all` renders both figures from these
CSVs in four formats each and writes a digest manifest to `manuscript/figures/MANIFEST.txt`. Its
Python dependencies are pinned in `manuscript/requirements.txt`.

## What is not here

Per-engine annotation outputs and per-machine diff reports total hundreds of gigabytes and are not
distributed. The aggregates every published figure is computed from are the files above, so no
published number depends on access to them. One consequence: the per-class attribution of residual
discordance cannot be re-derived from this directory.

Per-machine identifiers in the provenance records, and in the `clone` column of
`fastvep_extra_tuples_by_transcript.csv` and `fastvep_zero_f1_shapes.csv`, are anonymous labels assigned
per architecture, `clone-arm64-NN` and `clone-x86_64-NN`, numbered in order of first appearance within a
record; the same machine carries the same label in every field of that record. Every measurement,
digest, median and percentile is as measured.
